export const getApiGreetings = () => {
  let message: string = "Greetings from Biniverse API v1.0.0";
  return message;
};

// TODO: If DB data is available create service that will connect and communicate with the DB service
